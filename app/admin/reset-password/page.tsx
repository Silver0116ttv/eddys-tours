import { ResetPassword } from '@/components/admin/reset-password'
export const metadata = {
  title: 'Recuperar acceso | Eddy’s Tours',
  robots: { index: false, follow: false },
}
export default function Page() {
  return <ResetPassword />
}
